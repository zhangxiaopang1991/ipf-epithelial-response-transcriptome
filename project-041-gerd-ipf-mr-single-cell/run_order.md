# Project 041 run order

Run from `project-041-gerd-ipf-mr-single-cell/` after installing the documented R and Node dependencies. Input and output paths are resolved relative to the working directory. Large public files are intentionally not versioned.

The primary claim is an association between genetically predicted GERD liability and IPF risk. Single-cell outputs are donor-level exploratory localization, and the reverse MR direction is limited by instrument matching.
